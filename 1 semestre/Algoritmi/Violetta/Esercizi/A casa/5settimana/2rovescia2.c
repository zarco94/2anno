#include <stdio.h>
#include <stdlib.h>

int main(void){
    int *p, *temp;
    int n;
    int i = 0;
    int size = 15*sizeof(int);

    p = malloc(size);

    do{
        scanf("%d", &n);
        p[i] = n;
        i++

        if(i>=)
    }while(n!=0);
}